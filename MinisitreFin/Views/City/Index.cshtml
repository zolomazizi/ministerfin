@model IEnumerable<MinisitreFin.Models.CityModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_dashboardAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Creat")
</p>
<table class="table">
    <tr>
        <th>
           ville
        </th>
        <th>
            diametre
        </th>
        <th>
            Nombre Quartier
        </th>
        <th>
            Max Scan  
        </th>
        <th>
            Min Scan
        </th>
        <th>
            Status du City
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.City1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.diametre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Quartier)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Max_Scan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Min_Scan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status_City)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.ID }) |*@
            @*@Html.ActionLink("Delete", "Delete", new { id=item.ID })*@
        </td>
    </tr>
}

</table>
